Timestamp: 2026-09-10 23:19:04 -05:00 (America/Chicago)

Sure, go ahead and fix these two issues. **History refresh must update existing messages.** A user message can change from `in_progress` to `failed` or `interrupted` without a new message being added. Fetching only newer sequences will miss that change. Specify how reconciliation refreshes existing request status, and prevent stale reads from overwriting terminal outcomes.

1. **Missing conversations need an escape from the pending-state lock.** B.2 offers New chat when a conversation is missing, while B.6 disables it for unresolved requests. After a development database reset, a saved pending request could trap the UI. Explicitly allow recovery after a definitive 404, preserving the text as a draft without automatically resending it.