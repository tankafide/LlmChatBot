2026-09-10 22:58:23 -05:00 America/Chicago

Okay, can you address these issues in a best practice way? First, confirm that they are indeed issues, and if so, fix them.  **[P1] Admission can leave a conversation stuck until restart.** In [service.py (line 379)](/C:/Users/shane/Documents/Apps/LlmChatBot/backend/src/autoassist/conversations/service.py:379), cancellation stops awaiting the database thread without stopping its commit. Because `admitted` remains unset, interruption settlement is skipped. I reproduced persistent `request_in_progress` on retries and `conversation_busy` for new messages, with zero provider calls. An injected error after admission commit produces the same result. Track outstanding admission work and reconcile its durable outcome before releasing the turn.

1. **[P2] Selection validates evidence, but not the user’s choice.** [grounded.py (line 295)](/C:/Users/shane/Documents/Apps/LlmChatBot/backend/src/autoassist/chat/grounded.py:295) accepts any retrieved vehicle as the selection. A model can display several results and silently select one, or select a different vehicle from the requested list number. Validate selection against the explicit stock/list reference or existing selected context.
2. **[P2] Trimmed lists remain available for ordinal references.** [repository.py (line 92)](/C:/Users/shane/Documents/Apps/LlmChatBot/backend/src/autoassist/conversations/repository.py:92) restores presentation IDs before the runner applies its 64 KiB history limit. Consequently, “the second one” can resolve against a list whose turn was discarded. Derive presentation context from the same retained history suffix.
3. **[P2] The total provider-input limit isn’t enforced per request.** [grounded.py (line 210)](/C:/Users/shane/Documents/Apps/LlmChatBot/backend/src/autoassist/chat/grounded.py:210) checks initial history and prompt only. Tool schemas, subsequent model messages, repair messages, and serialization overhead aren’t covered by that check. The separate tool-result allowance doesn’t establish the promised 128 KiB total bound. Check actual assembled input before each provider call.
