Timestamp: 2026-09-10 22:45:26 -05:00 (America/Chicago)

Sure, fix these two items. **Provide one complete verification command.** Currently, the “shared” command excludes required container acceptance, which runs separately. Use one host wrapper for both, and invoke it consistently from CI and clean-extraction verification.

1. **Specify how the shared scenario runs in both environments.** ASGI tests use Python dependencies, while the host harness promises standard-library-only execution. Define a small HTTP adapter boundary and split the scenario into before/after-restart stages so lifecycle ownership is clear.
