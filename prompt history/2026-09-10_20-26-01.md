# User prompt

2026-09-10 20:26:01 -05:00 America/Chicago

Okay, let's go ahead and make these two updates.  &#x20;



**Specify database execution and session ownership.**\
“Use synchronous database access consistently” leaves an important implementation choice open when combined with async orchestration. Require blocking database operations to run outside the event loop, sessions to remain local to their execution context, and transactions to finish before waiting on external services. Pydantic AI can execute tools concurrently; sharing one SQLAlchemy session between those tools would be unsafe. This needs an explicit rule and focused tests. [FastAPI](https://fastapi.tiangolo.com/async/), [SQLAlchemy](https://docs.sqlalchemy.org/en/20/orm/session_basics.html#is-the-session-thread-safe-is-asyncsession-safe-to-share-in-concurrent-tasks), [Pydantic AI](https://pydantic.dev/docs/ai/tools-toolsets/tools-advanced/#parallel-tool-calls-concurrency)

1. **Define the lifecycle of a chat request.**\
   Restart durability is covered, but several observable behaviors remain undecided: two simultaneous messages in one conversation, a provider failure after saving the user message, and a client retry after losing the response. Add a small acceptance table describing what gets persisted and returned in each case. Keep the implementation appropriate to the prototype; no durable workflow engine is necessary.